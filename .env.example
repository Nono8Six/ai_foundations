# ==============================================================================
# Variables d'environnement pour le développement local avec Docker Compose
# Copiez ce fichier en .env et ne le commitez JAMAIS sur Git.
# ==============================================================================

# ─── Configuration Locale Supabase ──────────────────────────────────────────
# Ces variables sont utilisées par le service `supabase` dans docker-compose.yml
# Vous pouvez changer ces mots de passe si vous le souhaitez.
SUPABASE_DB_PASSWORD=your-super-secret-and-long-postgres-password
SUPABASE_JWT_SECRET=your-super-secret-and-long-jwt-secret-with-at-least-32-characters

# ─── Connexion du Frontend à la base de données locale ─────────────────────
# Ces variables sont utilisées par le service `frontend` pour se connecter
# à la base de données PostgreSQL locale dans Docker.
# Note: L'URL et la clé sont des exemples, à adapter selon votre configuration.
VITE_SUPABASE_URL=http://localhost:5433
VITE_SUPABASE_ANON_KEY=your-local-anon-key

# ─── Application ────────────────────────────────────────────────────────────
VITE_APP_NAME=IA Foundations
VITE_APP_ENV=development

# ─── Debug & logs ───────────────────────────────────────────────────────────
VITE_DEBUG=true
VITE_LOG_LEVEL=debug

# ─── PGAdmin (Optionnel) ────────────────────────────────────────────────────
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin