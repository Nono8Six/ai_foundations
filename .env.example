# =============================================================================
# AI Foundations - Environment Variables (Cloud-First)
# Copy this file to .env and fill in your Supabase cloud credentials
# 🚫 NEVER commit .env to git - it's in .gitignore for security
# =============================================================================

# =============================================================================
# SUPABASE CLOUD CONFIGURATION 
# Get these from your Supabase project dashboard: https://supabase.com/dashboard
# =============================================================================

# Your Supabase project URL (found in Settings > API)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Public anon key (safe to expose to frontend)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key (keep secret, used for server-side operations)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Project reference ID (used for CLI operations)
SUPABASE_PROJECT_REF=your-project-ref

# Personal access token for Supabase CLI
# Generate at: https://supabase.com/dashboard/account/tokens
SUPABASE_ACCESS_TOKEN=your-personal-access-token

# Database password (if needed for direct connections)
SUPABASE_DB_PASSWORD=your-db-password

# JWT secret (found in Settings > API)
SUPABASE_JWT_SECRET=your-jwt-secret

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Application metadata
VITE_APP_NAME="AI Foundations"
VITE_APP_ENV="development"

# Logging configuration
VITE_LOG_LEVEL=info
LOG_LEVEL=info
VITE_DEBUG=true

# Node.js optimization
NODE_OPTIONS=--max-old-space-size=4096

# =============================================================================
# 🤖 AI TOOLS NOTES
# =============================================================================
# This setup connects directly to Supabase cloud - no Docker required!
# 
# Quick Setup:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your Supabase cloud credentials above
# 3. Run: pnpm dev
# 4. Start coding with AI tools having consistent access to the same data!
#
# Benefits for AI development:
# ✅ Single source of truth (cloud database)
# ✅ No complex Docker setup
# ✅ Consistent environment across AI sessions
# ✅ Real-time collaboration capabilities
